import React from "react";

const TitleNames = ({ name, subtitle }) => {
    return (
        <div>
            <p>title : {name}</p>
            <p>subtitle : {subtitle}</p>
        </div>
    );
};

export default TitleNames;
